# InstaBio Environment Configuration
# Copy to .env and fill in your values

# Server
HOST=0.0.0.0
PORT=8000

# Token expiration (days)
TOKEN_EXPIRY_DAYS=90

# Database path (defaults to ./data/instabio.db)
# DATABASE_PATH=./data/instabio.db

# Veron server (SadTalker, Ollama, TTS)
VERON_AVATAR_API=http://YOUR_SERVER_IP:8100
VERON_OLLAMA_API=http://YOUR_SERVER_IP:11434
VERON_TTS_API=http://YOUR_SERVER_IP:8100

# Production settings
# FORCE_HTTPS=1
