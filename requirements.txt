# InstaBio Phase 1 - Recording MVP
# Python 3.10+ required

# Web framework
fastapi==0.129.0
uvicorn[standard]==0.40.0

# Audio processing & transcription
faster-whisper==1.2.1  # Local offline transcription — no GPU needed (uses int8 CPU)

# File uploads
python-multipart==0.0.22

# WebSocket support (for real-time transcription)
websockets==10.4

# Database (SQLite is stdlib, but we need async support)
aiosqlite==0.22.1

# Rate limiting
slowapi==0.1.9

# Security
bcrypt==5.0.0  # PIN hashing

# Utilities
pydantic==2.12.5  # Data validation
email-validator==2.3.0  # Email validation for pydantic

# Testing
pytest==9.0.2
httpx==0.28.1
pytest-asyncio==1.3.0
